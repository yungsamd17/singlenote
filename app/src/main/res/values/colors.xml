<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Launch window: covers the cold-start gap before first draw so the
        app opens onto its own surface instead of a gray void. -->
    <color name="launch_background">#FFFBFE</color>
    <!-- Dark twin of the above, picked in MainActivity to match the stored
        theme before the first frame. -->
    <color name="launch_background_dark">#FF141218</color>
</resources>
