## What Changed

<!-- One concern per PR, in plain words — what did you change? If this says "also", split it. -->

## Why

<!-- What problem does this solve for users? Link the TODO item or issue it closes. -->

## Evidence

<!-- No UI change: delete this section. UI change: before/after screenshots;
     motion/timing: short video. Upload directly to GitHub — never commit PR-only assets. -->

## Touched Areas

<!-- Bullet list of files/areas, e.g. `NoteScreen.kt`, `strings.xml`, `build.yml`. -->

## Testing

<!-- How was this verified? Tick before merge. Local sandboxes lack the SDK — CI covers build/tests. -->

- [ ] CI build passes
- [ ] Tests/lint pass
- [ ] Verified the change works as described

Built with {model} in the {agent} harness.
